# this code is to find the prime factors of input value

N = int(input("Prime factor of: ")) # to prompt for value 

# def get_prime_factors(N):
factors = list()
divisor = 2
while (divisor <= N):
    if (N % divisor) == 0:
        factors.append(divisor)
        N = N/divisor
    else:
        divisor += 1
print("prime factor is " + str(factors)) 
